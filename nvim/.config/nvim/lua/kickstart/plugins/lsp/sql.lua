-- SQL Language Server Configuration

return {
    sqls = {
        settings = {
            sqls = {
                connections = {
                    -- Add your database connections here
                    -- {
                    --   driver = 'postgresql',
                    --   dataSourceName = 'host=localhost port=5432 user=postgres password=postgres dbname=mydb',
                    -- },
                },
            },
        },
    },
}
